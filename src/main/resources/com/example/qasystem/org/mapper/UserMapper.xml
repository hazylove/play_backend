<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.qasystem.org.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id"  parameterType="User">
        insert into t_user(
                           username, password, phone, email, created_date
        ) VALUE (
            #{username}, #{password1}, #{phone}, #{email}, #{createdDate}
            )
    </insert>
    <select id="getUserIdByUsername" resultType="java.lang.Long">
        select
            id
        from t_user
        where username = #{username}
        limit 1
    </select>
    <select id="getPasswordByUsername" resultType="java.lang.String">
        select
            password
        from t_user
        where username = #{username}
        limit 1
    </select>
</mapper>