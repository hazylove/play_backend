<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.qasystem.user.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id"  parameterType="UserRegistration">
        INSERT INTO t_user(
                           username, password, phone, email, created_date
        ) VALUE (
            #{username}, #{password1}, #{phone}, #{email}, #{createdDate}
            )
    </insert>
    <select id="getUserIdByUsername" resultType="java.lang.Long">
        SELECT
            id
        FROM t_user
        WHERE username = #{username}
        LIMIT 1
    </select>
    <select id="getPasswordByUsername" resultType="java.lang.String">
        SELECT
            password
        FROM t_user
        WHERE username = #{username}
        LIMIT 1
    </select>
    <select id="getUserByUsername" resultType="User">
        SELECT
            id, username, password, phone, email
        FROM t_user
        WHERE username = #{username}
        LIMIT 1
    </select>
    <select id="selectUserInfoById" resultType="UserBase">
        SELECT
            id, username
        FROM t_user
        WHERE id = #{id}
        LIMIT 1
    </select>
</mapper>